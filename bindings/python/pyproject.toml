[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "tui-zig"
version = "1.0.0rc2"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/tui_zig"]
